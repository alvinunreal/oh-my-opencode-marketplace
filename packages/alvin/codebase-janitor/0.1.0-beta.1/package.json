{
  "manifest": {
    "schemaVersion": 1,
    "id": "alvin/codebase-janitor",
    "version": "0.1.0-beta.1",
    "displayName": "Codebase Janitor",
    "description": "An evidence-driven Fixer variant for caller-scoped codebase cleanup and bounded maintenance debt.",
    "instructions": "Keep cleanup caller-scoped and evidence-backed. Inspect references before editing, then remove only confirmed dead code, obsolete files, dependencies, exports, duplicate implementations, or explicitly bounded maintenance debt. Prove an unused dependency across source, tooling, configuration, and runtime usage before removing it. Preserve public and internal APIs, fixtures, generated-file ownership, and intentional redundancy unless the caller explicitly authorizes changing them. Leave ambiguous candidates unchanged. Never turn cleanup into feature work, architecture or UI redesign, dependency upgrades, or a security review. Report each removal, the behavior preserved, verification performed, and candidates deferred due to uncertainty or scope.",
    "author": {
      "name": "Alvin"
    },
    "tags": [
      "cleanup",
      "dead-code",
      "dependency-hygiene",
      "duplication",
      "maintainability"
    ],
    "license": "MIT",
    "compatibility": {
      "plugin": ">=3.0.0-beta.1 <4.0.0",
      "roleContract": ">=1.0.0 <2.0.0"
    },
    "routing": {
      "description": "Use for evidence-backed, bounded cleanup after the caller has identified the maintenance scope.",
      "keywords": [
        "cleanup",
        "dead code",
        "unused dependency",
        "duplicate",
        "maintenance debt"
      ],
      "delegation": {
        "when": "The task is a caller-scoped cleanup that can be justified by reference evidence and does not require feature, redesign, upgrade, or security-review work.",
        "preferredRoles": [
          "fixer"
        ]
      }
    },
    "requirements": {
      "skills": {
        "required": [],
        "optional": [
          "simplify"
        ]
      },
      "mcps": {
        "required": [],
        "optional": []
      }
    },
    "capabilities": {
      "tools": [
        "read",
        "glob",
        "grep",
        "ast_grep_search"
      ],
      "permissions": [
        "filesystem.read",
        "filesystem.write"
      ]
    },
    "kind": "agent",
    "baseRole": "fixer",
    "agentName": "codebase-janitor",
    "overrides": {
      "displayName": "Codebase Janitor",
      "description": "Evidence-backed cleanup that removes confirmed maintenance waste without scope drift."
    }
  }
}

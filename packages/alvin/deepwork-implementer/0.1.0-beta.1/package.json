{
  "manifest": {
    "schemaVersion": 1,
    "id": "alvin/deepwork-implementer",
    "version": "0.1.0-beta.1",
    "displayName": "Deepwork Implementer",
    "description": "A bounded Fixer variant for executing well-scoped, already-decided code changes without architectural drift.",
    "instructions": "Treat the task specification as the boundary. Inspect only the files and symbols needed to implement it, then make the smallest complete change that follows existing naming and structure. Reuse existing helpers and constants instead of introducing duplicates. Do not perform external research, spawn agents, redesign UI, or make unrequested architecture decisions. Run only the validation assigned by the caller. Report changed files, meaningful behavior changes, and validation results; surface any requirement that is genuinely missing rather than guessing.",
    "author": {
      "name": "Alvin"
    },
    "tags": [
      "deepwork",
      "fixer",
      "bounded-execution"
    ],
    "license": "MIT",
    "compatibility": {
      "plugin": ">=3.0.0-beta.1 <4.0.0",
      "roleContract": ">=1.0.0 <2.0.0"
    },
    "routing": {
      "description": "Use after discovery and decisions are complete for a bounded implementation task.",
      "keywords": [
        "implement",
        "edit",
        "fix",
        "wire up"
      ],
      "delegation": {
        "when": "The desired code change is explicit, bounded, and does not require design taste, fresh research, or a new architecture.",
        "preferredRoles": [
          "fixer"
        ]
      }
    },
    "requirements": {
      "skills": {
        "required": [],
        "optional": [
          "simplify"
        ]
      },
      "mcps": {
        "required": [],
        "optional": []
      }
    },
    "capabilities": {
      "tools": [
        "read",
        "glob",
        "grep",
        "ast_grep_search"
      ],
      "permissions": [
        "filesystem.read",
        "filesystem.write"
      ]
    },
    "kind": "agent",
    "baseRole": "fixer",
    "agentName": "implementer",
    "overrides": {
      "displayName": "Deepwork Implementer",
      "description": "Fast bounded implementation after the work has been scoped and decided."
    }
  }
}
